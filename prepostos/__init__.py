"""
Pacote do app `prepostos`.

Este arquivo marca o diretório como pacote Python e permite que o Django
importe os módulos do app (models, views, urls e admin) quando o app é
registrado no `INSTALLED_APPS`.
"""
