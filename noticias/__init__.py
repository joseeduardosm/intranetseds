"""
Pacote do app `noticias`.

Este arquivo marca o diretório como pacote Python importável. Na arquitetura
do Django, permite o carregamento dos módulos internos (`apps`, `models`,
`views`, `urls`, `admin`) quando o app é referenciado em `INSTALLED_APPS`.
"""
